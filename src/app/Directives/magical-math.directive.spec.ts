import { MagicalMathDirective } from './magical-math.directive';

describe('MagicalMathDirective', () => {
  it('should create an instance', () => {
    const directive = new MagicalMathDirective();
    expect(directive).toBeTruthy();
  });
});
